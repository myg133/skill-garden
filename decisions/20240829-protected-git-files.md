# 决策日志：受保护的 Git 追踪文件

**日期**: 2024-08-29  
**决策者**: anspire-open-ba  
**状态**: 已记录，持续生效

## 约束内容

在当前仓库 `D:/MyCodes/Rust/skill-garden/` 中，以下文件/目录**目前在 Git 中被追踪**，**绝对禁止删除**：

| 路径 | 说明 |
|------|------|
| `.pi/` | Pi Agent 运行时的元数据目录（sessions、tasks、skills 等） |
| `args` | Agent 启动参数文件 |

## 风险说明

- **误删除风险**: 如果执行 `git clean`、`git reset --hard` 或手动删除，可能导致：
  - 会话历史丢失
  - 任务状态丢失
  - Agent 配置损坏

## 预防措施

1. **所有清理操作前**必须先检查这些路径是否存在
2. 使用 `gstack-careful` skill 进行安全检查
3. 禁止在 `git add .` 后直接 commit 包含这些文件的变更
4. 如需迁移这些文件，**必须先在 git 中取消追踪，再移动**，然后重新追踪新位置

## 违反此约束的后果

⚠️ **高风险**: 删除后可能导致当前 Agent 会话状态丢失、任务追踪中断、历史记录不可恢复。

---

**下次维护时复查**: 每次启动 BA Agent 时自动确认
