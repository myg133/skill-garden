{{/*
  AionHive ServiceAccount
*/}}
{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "aion-hive.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aion-hive.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
